- win PCで環境変数の設定
- winでeclipse + spring boot でプロジェクト作成
- winでmysqlローカルで基本SQL文つかう
- winからgithubにpush & pull 
- render にava + SQliteで簡単なアプリデプロイ
- railway無料で(java + mysql)のアプリデプロイ　<ーもうサービス停止
- win にdocker desktopいれる　（当時なにがなんだかわからない）
- win でAWS サインアップ
- win でAWS　EC2のインスタンス作成（でも意味わからんかった）
- 低スペlaptopにlinux ：lubuntuを入れてみる（初心者にしては設定が難しかった。）
- 低スペlaptopにlinux ：linux mintをインストール(設定も情報も簡単で豊富)<- 現在のメインOS
- linux mint で日本語入力設定fcitx + mozc をすべて入れ直しで改善
- laptopでvscodeインストール（desktopとおなじ開発をするため）
- vscode拡張機能インストール
-  linux mint でjdk install
- linux mint でgit install
- linux mint でAnki install
- linux mint  でObsidian install(Notionはおもい)
- linux miint でdocker engine install(desktopはおもい)
- フレームワークが一緒ならどんなIDEでも動かせることに気づく（依存関係はしっかり見る必要がある）
- githubからdesktopのプロジェクトpull (データの位置を決めるためパスに移動が初歩)
- linux mint でmariadb install 
- linux mint でプロジェクトのapplication.propertiesでlaptopとdesktopでテスト（実行）できるように修正
- git push (ここでなぜかuser.nameとtokenの入力要求が必要になった)
- git pushをlinuxのgit config --global certidentce~なんちゃらでuser.name tokenを記憶させるよう設定
-  プロジェクトをコンテナ化したいのでdockerfile,docker-compose.ymlを作成
- laptopでテスト:docker compose up --build で成功
- git push 
- git branch で新しくtest(experience)のブランチ作成
- branch experienceでgit push (今後はこっちですすめていく)
